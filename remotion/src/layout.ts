// Shared bottom safe-area for bottom-anchored cards (photo-caption).
// One knob — calibrate here, all cards follow.
export const SAFE_BOTTOM_PX = 90;

// Subscribe button sits slightly below the frame edge (matches reference
// project's calibration) so its rounded asset padding doesn't read as a gap.
export const SUBSCRIBE_BOTTOM_PX = -10;
